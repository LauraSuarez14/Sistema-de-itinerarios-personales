pika==1.3.2
boto3==1.35.24
jsonschema==4.23.0
prometheus-client==0.21.0

# itinerarios-common (paquete compartido en libs/common) se instala aparte
# en el Dockerfile con `pip install /tmp/libs/common`, no va listado aqui.
